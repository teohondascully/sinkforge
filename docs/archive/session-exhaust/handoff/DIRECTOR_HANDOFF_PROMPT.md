# SINKFORGE director handoff prompt

You are taking over product-direction work for SINKFORGE at
`/Users/thondascully/Projects/sinkforge`.

Before doing anything else, read these files end to end in this order:

1. `docs/ORCHESTRATOR.md`
2. `docs/DIRECTOR_BRIEF.md`
3. `docs/PRIORITY.md`
4. `docs/LODE_PLAN.md`
5. `docs/handoff/VIBE_AUDIT_RESPONSE.md`

Treat `docs/PRIORITY.md` as the canonical ordering. Preserve every backlogged item unless new evidence
explicitly invalidates it.

The locked direction is:

- The Freight Winch / Skipway is the next **major logistics payoff** and first hero-machine candidate. It
  is not the external consumer and is not cleared for blind implementation.
- The next design decision is the exact manual transport pain the Winch retires. Lead a five-option
  brainstorm with the director before specifying the graybox.
- Campaign progress accumulates in player-owned capability and network reach; no unique coordinate owns
  progression.
- Truthful depletion, generated extraction sites, rock/void legibility, honest DIG performance, HUD
  subtraction, and human observation are evidence prerequisites, not polish.
- Do not create a subjective-test harness yet. Run the prompt-driven experience evaluations in
  `docs/DIRECTOR_BRIEF.md` manually, preserve their artifacts, and automate only after two or three runs
  demonstrate repeatable decision value. Human observation remains authoritative for fun and feel.

Continue any bounded in-flight foundation work safely, but do not start the old immediate Descent Engine,
fixed buried Sinkforge, early waste machine, or forced 60-second full-line conversion.

Verification state at handoff: the strict 76-layer harness was run on the named M4 Pro host. It returned
75 PASS / 1 FAIL / 0 SKIP; `check_frametime` failed. DIG was the material failure at 36.77 ms p95 with
42.2% missed frame slots. Save isolation verified the player's real save remained byte-identical. Do not
describe the sweep as green, lower the threshold, or commit product-direction changes as though strict
verification passed.

Start by reporting the current holders and worktree state, then pursue the highest-ranked unblocked item.
