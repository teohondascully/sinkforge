# Screen-led playtest, stranger 4 -- 2026-09-06, after D0424

**Who played:** a fresh-context agent (Claude Haiku 4.5), the same mission as strangers 1-3 plus one
sentence about the figure and the scrolling world (stranger 3 read left for right). No source, no
coordinates, no coaching. 35 bursts and a quit, 76 tool calls, 7.4 minutes of wall time, 71 s of game time.
Journal: `docs/playtests/2026-09-06_stranger4_journal.md` (the agent summarised bursts 2-35 in blocks
instead of one entry a burst); frames and the input log: `tests/body/recordings/playtest_2026-09-06_stranger4/`
(local).

**What changed between runs 3 and 4 (D0424):** the grapple's landing ring gated and hemp; signed, named
payout ticks; the how-to wrapping; a hidden lesson yielding.

## Timeline (game seconds)

| Milestone | S1 | S2 | S3 | S4 | Note |
|---|---|---|---|---|---|
| First ore | 10.7 | 6.5 | 4.0 | 1.0 (burst 1) | one hold on the ringed rock; no ring confusion this time |
| First ingots | 26.1 | 11.4 | never | by 14.5 (burst 12) | "-5 ore" seen at the drop, "Forge lit up with flames" |
| First wood | -- | -- | -- | not reached | the ring found a trunk; the agent clicked it from 12 m, then wandered |
| First automation | -- | -- | -- | not reached | |

Stopped by its own decision after burst 35, 46 m down, after 7 bursts of grappling climbed 4 m.

## What the stranger said

- Burst 9: "Pressed Q to feed ore to forge. Saw '-5 ore' float up. Forge lit up with flames."
- On the wood rung: "Initial screenshot showed two trees at surface level near spawn. When player later
  encountered a third tree at elevated TOPSOIL, it was the one mentioned in objective."
- "Walking right from TOPSOIL led downward into deep caves rather than to the elevated tree."
- "Once deep, very hard to get back."
- Verdict: "Would keep playing: yes, but with reservations ... one wrong turn traps you for many
  minutes." Biggest obstacle: "ambiguous terrain navigation and verticality."

## What the frames show

- **The D0424 tick did its job.** The drop beside the forge read as feeding ("-5 ore", the flames), not as
  a forge failing; the opening frame's only pale-gold ring was the tutorial's.
- **The wood rung has no target on the pad.** The site keeps worldgen trees 12 m off the spawn; at this seed
  the nearest trunks stand 22 m either side, one across the 20 m shaft. `TargetGuide` searches 10 m, so
  the rung opened with no ring. Legacy's tutorial planted a guaranteed tree beside the spawn for exactly
  this reason ("depth-banded worldgen plants trees out past the ruin, unreachable on the surface early");
  D0353 did not carry it.
- **The agent crossed the shaft and passed the tree.** Frame 17: the body stands on the far ledge with the
  wood ring on a trunk 12 m behind it; bursts 18-19 hold MINE on that trunk from there (TOO FAR, 7 s);
  then D, Space+D, A -- and the fall into the shaft (burst 23, 37 s), the same one stranger 3 fell into.
- **The grapple worked and was slow in the agent's hands.** GRAPPLE at 50 s, CHAIN IT at ~60 s; 4 m in
  7 bursts. The lesson arrived within a few seconds of the depth this time (D0424's yield).

## Decided from this run (D0425)

1. Legacy's guaranteed tutorial tree is back, as a `tree` start fixture planted through the world's own
   tree pass (the same shape as every other tree), six metres left of spawn on the pad, on the side away
   from the shaft. The wood rung has a ringed target from where the smelt rung ends.

## Named, not done

- The shaft beside the pad swallowed both strangers who reached the wood rung (T031, now with two runs).
- The rung's target ring is only on screen; nothing points at a target off the edge.
- Four runs of four agents. The ore time has fallen every run (10.7, 6.5, 4.0, 1.0 s) as the pointer and
  the marks were fixed; the forge was reached by three of four; no run has felled a tree.
