# Screen-led playtest, stranger 3 -- 2026-09-06, after D0423

**Who played:** a fresh-context agent (Claude Haiku 4.5), the same mission as strangers 1 and 2. No source,
no coordinates, no coaching. 35 bursts and a quit, 81 tool calls, 9.6 minutes of wall time, 65 s of game time.
Journal: `docs/playtests/2026-09-06_stranger3_journal.md`; frames and the input log:
`tests/body/recordings/playtest_2026-09-06_stranger3/` (local).

**What changed between runs 2 and 3 (D0423):** the cut's progress fills the aim square from the floor up.
The wood rung was never reached, so this run says nothing about it.

## Timeline (game seconds)

| Milestone | Stranger 1 | Stranger 2 | Stranger 3 | Note |
|---|---|---|---|---|
| First ore | 10.7 s | 6.5 s | 4.0 s (burst 2) | one fruitless hold first (at its own feet); then 7 ore in one 2 s hold |
| First ingots | 26.1 s | 11.4 s | not reached | the forge was never fed: every DROP was 5 m or more from it |
| First wood | not reached | not reached | not reached | |
| First automation | not reached | not reached | not reached | |

Stopped by its own decision after burst 35, 50 m deep, "lost navigating deep underground zones".

## What the stranger said

- Burst 1: "Unclear which of the yellow circles is the actual 'RINGED silver-flecked rock.' There are
  multiple yellow rings visible on screen."
- Burst 3: "Instruction text ... 'then wait: the ingo...' (text cut off, likely says 'ingots')."
- Bursts 5-8: "Ore inventory UI disappeared ... Gear/wheel icon appeared in the forge area, suggesting it's
  processing" -- then "Ore inventory UI reappeared at bottom, still showing 7 ore ... No error message
  displayed."
- Burst 20: "Discovered drag-and-drop mechanic by dragging ore inventory to forge ... Right: '3'
  (brown/orange icon)."
- Report: "the forge objective was frustrating because the UI showed items being created yet the game
  never explained how to register them toward the goal, making the game feel either broken or
  deliberately obscure."
- Verdict: "I would keep playing if the tutorial and feedback were clearer about what each UI element
  means."

## What the frames show

- **The brightest ring on the opening frame was the grapple's landing ghost.** Frame 0 has four rings:
  the tutorial's target ring on the vein (large, faint, breathing), two machine status pips (small ring
  and dot), and a bright pale-gold ring beside the miner's feet that is `RopePainter`'s aim ghost -- where
  a thrown hook would land, drawn from the first frame in a colour 0.02 from the target ring's ink, ten
  metres above the lesson that introduces the grapple. In frame 4 it sat on a silver-flecked lode at the
  hill's crest and looked exactly like "the RINGED silver-flecked rock".
- **The forge was to the RIGHT in frame 3 and the agent walked left.** Frame 2 shows the forge 1.5 m left
  of the miner; one second of walking left (burst 3) carried the miner 5 m past it; the agent then read
  frame 3 as "forge on the left" and walked another two seconds. This is the agent misreading a frame,
  not the game -- but the first second's overshoot is real: a body length of distance is a fifth of a
  second's walk.
- **DROP far from the forge is a silent round trip.** Q dropped the whole stack (legacy's verb) into the
  facing column; the hotbar vanished (an empty pack draws no bar, D0412); 1.3 s later the drop grace
  expired and the standing collect took it back; the hotbar returned. Nothing on screen said "dropped" or
  "collected". The "gear icon" was the surface's decorative half-buried cog, previously hidden behind the
  hotbar. The agent read the sequence as a forge processing and failing, twice.
- **The "drags" were MINE holds with the pointer moved.** Each drag began with LMB on the hotbar's screen
  position, which is rock under the miner in world space, so the drags dug: "11 ore" and "3" then "5" of
  a brown item (dirt), and yellow dashed dig-plan lines along every drag path (T030, again). No ingot was
  ever made.
- **Two seconds of walking right from the pad is a twenty-metre fall.** Burst 27: D for 120 ticks from
  beside the forge; frame 27 is 22 m down THE CLAYBAND. The GRAPPLE lesson (the way back) fired at 10 m
  but showed at burst 32 (~58 s): TOO FAR was on the plate from burst 24, the body was busy the whole
  fall and the walk after, so its calm countdown never ran, and the grapple lesson waited behind it for
  the linger.
- **The how-to was elided.** "Walk to the RINGED forge, stand beside it and press Q to feed it the ore,
  then wait: the ingo…" at 1280x720 with a real depth chip. The clause cut was the one that says the
  ingots come to you.

## Decided from this run (D0424)

1. The grapple's landing ring waits for the grapple to be known (the deep lesson given, or a line
   thrown), and is hemp-coloured; the tutorial's pale-gold ring is the only ring of its ink.
2. The payout tick names its item and a loss is a tick: "-7 ore" when the stack leaves the pack, "+7 ore"
   when it comes back. Resolves T029 by its third option.
3. The objective's how-to wraps to a second line before it gives with an ellipsis.
4. A lesson hidden by a busy body yields after five seconds when a ready lesson waits behind it; calm
   reading time is untouched.

## Named, not done

- The shaft beside the pad swallows a stranger who walks right for two seconds (T031).
- The machine status pips are rings too; whether the tutorial ring wants a distinct FORM, not only a
  distinct ink (T032).
- DROP is legacy's whole-stack verb; a tap dumps seven ore. Not changed: the stack is the recipe's unit
  and the ore came back.
- Three runs of three agents are three samples. The ore time fell 10.7 -> 6.5 -> 4.0 s across the runs
  and the fixes; the forge was reached by one of three. The wood rung has still never been played after
  D0423's gauge.
