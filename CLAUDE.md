# Sinkforge — start here

This file is auto-loaded at session start and survives context compaction. Its only job is to
point you at the documents that don't.

**Read, in order:** `CONTEXT.md` → `docs/GDD.md` → `docs/ARCHITECTURE.md` → `docs/QUALITY.md` →
`docs/CLAIMS.md` → `claims/C001-two-minute-run.md`. Then `docs/WORKING.md` for what's actually
happening right now — current stage, what's done, what's in flight, open questions.

If this is a fresh session or you just resumed from a compaction: read `docs/WORKING.md`, then
state in one paragraph what you're doing and why before touching anything. If that paragraph
doesn't match `docs/WORKING.md`, stop and say so rather than guessing forward.

Full protocol: `CONTEXT.md`, "Surviving context compaction" section.
