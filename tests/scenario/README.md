Tests that run a full scenario file from `scenarios/` through the harness driver and assert on the
result — end-to-end within the test suite, as distinct from `harness/scenario/`, which holds the
scenario format's loader and schema code itself, not tests of it.
